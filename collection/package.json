{
  "name": "@dnb-hugo-shortcodes/collection",
  "description": "Shortcodes for Hugo",
  "version": "1.4.1",
  "author": "Patrick Kollitsch <patrick@davids-neighbour.com>",
  "bugs": {
    "url": "https://github.com/dnb-hugo/shortcodes/issues"
  },
  "devDependencies": {
    "@dnb-hugo/remark-config": "2.0.3",
    "@dnb-hugo/standard-version-config": "2.0.5",
    "@slightlyoff/lite-vimeo": "0.1.2",
    "standard-version": "9.2.0"
  },
  "homepage": "https://github.com/dnb-hugo/shortcodes",
  "license": "MIT",
  "private": true,
  "scripts": {
    "release": "standard-version --release-as patch -a -t \"collection/v\" && git push --follow-tags origin main",
    "release-major": "standard-version --release-as major -a -t \"collection/v\" && git push --follow-tags origin main",
    "release-next": "standard-version --release-as minor -a -t \"collection/v\" && git push --follow-tags origin main",
    "remark": "remark"
  }
}
