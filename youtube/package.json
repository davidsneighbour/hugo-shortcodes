{
  "name": "@dnb-hugo-shortcodes/youtube",
  "description": "Youtube shortcode for Hugo",
  "version": "1.0.1",
  "author": "Patrick Kollitsch <patrick@davids-neighbour.com>",
  "bugs": {
    "url": "https://github.com/dnb-hugo/shortcodes/issues"
  },
  "devDependencies": {
    "@dnb-hugo/remark-preset-lint": "github:dnb-hugo/remark-preset-lint",
    "@dnb-hugo/standard-version-config": "github:dnb-hugo/standard-version-config",
    "@slightlyoff/lite-vimeo": "0.1.2",
    "standard-version": "9.2.0"
  },
  "homepage": "https://github.com/dnb-hugo/shortcodes",
  "license": "MIT",
  "private": true,
  "scripts": {
    "release": "standard-version --release-as patch -a -t \"youtube/v\" && git push --follow-tags origin main",
    "release-major": "standard-version --release-as major -a -t \"youtube/v\" && git push --follow-tags origin main",
    "release-next": "standard-version --release-as minor -a -t \"youtube/v\" && git push --follow-tags origin main",
    "remark": "remark"
  }
}
